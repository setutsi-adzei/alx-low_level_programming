#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: Returns zero - (success)
 */

int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'd';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'Z') {
			putchar(c);
			d++;
		}
	putchar('\n');
	return (0);
}
