#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: Returns zero 0 (succes)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
